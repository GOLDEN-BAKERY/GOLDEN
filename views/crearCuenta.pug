<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - Golden Bakery Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">

</head>
<body class="bg-black flex items-center justify-center h-screen">
    <div class="bg-white p-8 rounded shadow-md w-full max-w-lg border-4  border-dark-gold"> 
        <div class="bg-white p-8 rounded shadow-md w-full max-w-lg mt-14"> <!-- Añadí mt-12 para agregar margen superior -->
            <h2 class="text-4xl font-bold mb-4  text-center font-playfair text-dark-gold ">Crear Cuenta</h2>
        </div>
        <form action="/login" method="POST">
            <div class="mb-2 pt-4">
                <label for="name" class="block text-sm font-semibold mb-2 font-playfair">Nombre Completo</label>
                <input type="text" id="name" name="name" required class="border-4 px-4 py-2 w-full rounded">
            </div>
            <div class="mb-2 pt-2">
                <label for="email" class="block text-sm font-semibold mb-2 font-playfair">Correo Electrónico</label>
                <input type="email" id="email" name="email" required class="border-4 px-4 py-2 w-full rounded">
            </div>
            <div class="mb-2 pt-2">
                <label for="phone" class="block text-sm font-semibold mb-2 font-playfair">Número de Teléfono</label>
                <input type="tel" id="phone" name="phone" required class="border-4 px-4 py-2 w-full rounded">
            </div>
            <div class="mb-4 pt-2"">
                <label for="password" class="block text-sm font-semibold mb-2 font-playfair">Contraseña</label>
                <input type="password" id="password" name="password" required class="border-4 px-4 py-2 w-full rounded">
            </div>
        </form>
        <div class="flex items-center justify-between">
            <button type="submit" class="rounded-md bg-yellow-400 px-3.5 py-2.5 text-sm font-semibold text-black shadow-sm hover:bg-yellow-500">Registrarse</button>
        </div>

        <div class="mt-10 flex items-center justify-center gap-x-6">
            <a href="/" class="rounded-md bg-yellow-400 px-3.5 py-2.5 text-sm font-semibold text-black shadow-sm hover:bg-yellow-500">Pagina Principal</a>
        </div>

        <div id="logoGolden" class="flex justify-center items-end pb-4 mt-6">
            <img src="GOLDEN_logo.jpg" alt="Logo de Golden Bakery Shop" class="h-16 w-auto">
        </div>

       
    </div>
</body>
</html>
